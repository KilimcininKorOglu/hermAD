root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./bin/hermad ."
  bin = "./bin/hermad"
  include_ext = ["go", "html", "css", "json"]
  exclude_dir = ["docker-data", "bin", "tmp", "docs", ".git"]
  delay = 500
  stop_on_error = true

[misc]
  clean_on_exit = true
